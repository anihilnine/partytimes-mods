do
--  LOG("Hotbuild --> Loading keyactions (DISABLED, using UserKeyActions)")
--  local ka = import('/mods/hot/lua/hotbuild.lua').getKeyActions();
--    LOG("Loading hotbuild key descriptions: " .. repr(kd))
--  for key, action in ka do
--    keyActions[key] = action
--  end
end
